#!/bin/bash

# invoke python wrapper with Playbook path, and private data dir location
python3 minimal_ansible_playbook.py -p ./dynamicPlaybook2.yaml -d ./

# invoke python wrapper with Playbook path, and private data dir location
# python3 minimal_ansible_playbook.py -p ./dynamicPlaybook3.yaml -d ./

# invoke python wrapper with Playbook path, and private data dir location
# python3 minimal_ansible_playbook.py -p ./dynamicPlaybook.yaml -d ./


